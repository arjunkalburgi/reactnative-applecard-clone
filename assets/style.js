import { StyleSheet } from 'react-native';

const variables = StyleSheet.create({
    app: {
        margin: 15
    },
    box: {
        margin: 10,
        width: 'calc(100% - 20px)'
    },
});
export default variables;